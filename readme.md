## x-ray-detection

<hr>

- create a virtual env and run `pip install -r requirements.txt`

- create a local env files and add these credentials

  ```
  ROBOFLOW_API_KEY
  ```

- run `python main.py`
